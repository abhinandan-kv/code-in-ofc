const personTable =
  "(PersonID INT NOT NULL PRIMARY KEY AUTO_INCREMENT,LastName VARCHAR(255),FirstName VARCHAR(255),Address VARCHAR(255),City VARCHAR(255) NOT NULL,Country VARCHAR(255),Age INT NOT NULL);";

export default personTable;

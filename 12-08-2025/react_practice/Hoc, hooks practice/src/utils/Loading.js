import React from "react";

const Loading = () => (
  <div style={{ display: "flex", height: "100vh", alignItems: "center", fontSize: "15rem", fontFamily: "monospace", fontWeight: "bold" }}>
    Loading...
  </div>
);

export default Loading;
